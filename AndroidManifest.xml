<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.law.belarus.citizen.codex"
    android:versionCode="10"
    android:versionName="2.2.0" 
    android:installLocation="auto">

    <uses-sdk
        android:minSdkVersion="4"
        android:targetSdkVersion="19" />
    
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar"
        >
        <!--  android:theme="@style/AppTheme" --> 
        <activity
            android:name="com.law.belarus.citizen.codex.MainActivity"
            android:label="@string/app_name">
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.law.belarus.citizen.codex.EmailActivity"
            android:label="@string/dialog_email">
        </activity>
        <activity
            android:name="com.law.belarus.citizen.codex.SetTextSizeDialog"
            android:theme="@android:style/Theme.Dialog"
            android:label="@string/dialog_text_size" >
        </activity>
</application>

</manifest>
